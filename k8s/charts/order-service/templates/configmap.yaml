apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
  namespace: {{ .Values.namespace }}
data:
  sqsQueueUrl: {{ .Values.config.sqsQueueUrl }}
  awsRegion: {{ .Values.config.awsRegion }}
